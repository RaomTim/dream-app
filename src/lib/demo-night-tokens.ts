/**
 * @deprecated — REMPLACÉ par `src/lib/dream-design.ts` (design « NUIT ULTRA SIMPLE », chantier H, 2026-07-11).
 * Ce fichier est mort : plus aucune référence dans le code. Il ne subsiste que parce que le
 * sandbox n'autorise pas `rm` — à supprimer manuellement (`rm src/lib/demo-night-tokens.ts`).
 * Ne rien réimporter d'ici : les tokens vivent désormais dans dream-design.ts (T, DT, SCALE, MOTION…).
 */
export {}
